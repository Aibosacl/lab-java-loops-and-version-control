class ExerciseLabTask2 {
    public static void main(String[] args) {
        //Find the smallest and the second smallest this array
//Start from "0" to enable the check of the array from item on the list
        double[] element = {0.1, 0.2, 6.2, 5.8, 0.01, 9.2, 10.4, 3.3, 2.7};
                {
                System.out.println("array must have double element.");
                double smallest = element[0];
                double secondSmallest = element[0];
                for(int i = 1; i < element.length; i++) {
                    if(element[i] < smallest) {
                        smallest = element[i];

                        System.out.println("Smallest number in array: " + smallest);{
                            System.out.println("Secondsmallest element in array: " + secondSmallest);
                            {


                            }
                        }

                    }
                }
            }
    }
}